use("hello_world")
use("ispec")

describe("queen",
  it("should return a queen ant... or an ant queen",
    queen should mimic(Ant)
  )

  describe("after having gotten shugarz"
    before(
      Colony lotz(fn(ant,
        if(ant wawkz. ant hazFowndShugarz?,
          ant nomNom)
      ))
    )

    it("should puke hello world after having gotten shugarz",
      queen getz_shugarz
      queen puke should == "hello world"
    )
  )
)